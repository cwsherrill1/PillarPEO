/**
 * Build-time switches for placeholder scaffolding.
 * When SHOW_PLACEHOLDERS is false, ProofSlot and VerifyTodo render nothing at
 * all (no wrapper, no spacing), so pages can ship one at a time as real proof
 * and verified facts arrive.
 */
export const SHOW_PLACEHOLDERS = true;
